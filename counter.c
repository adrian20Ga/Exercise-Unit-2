#include <stdio.h>
#include <stdlib.h>

void main (int argc, char**argv){ //functions con parametros




	
	int limit= atoi(argv[1]); //convertir cadenas a enteros
			int i;
	
	
	
				for (i=1; i<= limit; i++) // ciclo para imprimir
	{
		printf("Counter is on %d \n", i);
	}
	
	getch
}

